name: local_notifications_service
description: A new Flutter module project.
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.5
  awesome_notifications: 0.7.4+1
  bottom_sheet_helper:
    git: https://github.com/niypoo/bottom_sheet_helper.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0